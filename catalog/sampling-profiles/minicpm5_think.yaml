# catalog/sampling-profiles/minicpm5_think.yaml
#
# MiniCPM5's thinking-mode profile.

label: minicpm5_think

temperature: 0.9
top_p: 0.95
top_k: -1                  # vLLM's own neutral default, written out
min_p: 0.0                  # rather than omitted.
presence_penalty: 0.0
repetition_penalty: 1.0
max_tokens: 16384

# Not cited in tool-call's own (partial) profile -- CATALOG Section 4.1
# and RESEARCH Section 16.3 both omit enable_thinking from their tables.
# Inferred from the profile name ("_think"), not read from
# tool-call's models.yaml. Revisit if that config is ever consulted
# directly.
enable_thinking: true

seed: 42   # S-D6.

# --- Provenance -----------------------------------------------------------
# Source: docs/STANDARDS_AND_PROFILES_CATALOG.md Section 4.1, row 7
# (tool-call models.yaml:78-80).
# Must hash to 9278fd8ea1b83470.
