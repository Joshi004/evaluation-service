"""Checkpoint controller.

Will validate registration requests (e.g. does the declared parent
checkpoint actually exist), orchestrate calls into app.services.s3 for
browsing/staging, and shape responses for app.api.v1.checkpoints. See
EVAL_SERVICE_PLAN.md, Section 7 and Section 10.

Not implemented yet.
"""
