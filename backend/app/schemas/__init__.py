"""Pydantic request/response schemas.

Empty for now. Will hold the API-facing shapes for checkpoints, benchmarks,
runs, leaderboard rows, etc. — kept separate from the ORM models in
app/models so the wire format can evolve independently of the schema.
"""
