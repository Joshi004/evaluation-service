export function RunsPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Runs</h1>
      <p className="mt-2 max-w-2xl text-slate-400">
        Everything in flight and recent. States, elapsed time, progress,
        live logs, truncation and error rates — the page people leave
        open. See EVAL_SERVICE_PLAN.md, Section 13.
      </p>
      <p className="mt-4 text-sm text-slate-500">Not implemented yet.</p>
    </div>
  )
}
