export function StandardsPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Standards / Methodology</h1>
      <p className="mt-2 max-w-2xl text-slate-400">
        The rendered recipe files: what each benchmark measures, every
        setting and where it came from, the changelog, and which
        reference number it was verified against. See
        EVAL_SERVICE_PLAN.md, Section 5 and Section 13.
      </p>
      <p className="mt-4 text-sm text-slate-500">Not implemented yet.</p>
    </div>
  )
}
